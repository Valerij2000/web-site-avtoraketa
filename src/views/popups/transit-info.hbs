<div class="modal modal-toast" id="info">
  <div class="modal__info modal-agent">

    {{!-- <button type="button" class="modal-close" data-dismiss="modal" aria-label="Close">
      ×
    </button> --}}

    <div class="modal-agent__image">
      <img src="@img/agent.jpeg" alt="Агент Авторакета">
    </div>

    <div class="modal-agent__content">
      <h3 class="modal-agent__title">
        Стань агентом<br>компании <span>Авторакета</span>
      </h3>

      <p class="modal-agent__text">
        Получай заявки и зарабатывай <span class="nowrap">вместе с нами</span>
      </p>

      <a href="/leads/" class="modal-agent__btn">🚀 Узнать подробности</a>

      {{!-- <a target="_blank" href="https://t.me/AvtoraketaLeads_bot?start=w45468305" class="modal-agent__btn">
        🚀 Перейти в Telegram
      </a> --}}

    </div>

  </div>
</div>

<style>
  .modal-agent {
    padding: 0;

    position: relative;
    max-width: 420px;
    margin: auto;

    background: #fff;
    border-radius: 24px;
    overflow: hidden;

    box-shadow: 0 30px 80px rgba(0, 0, 0, .25);

    animation: modalPop .5s ease;
  }

  /* Анимация появления */
  @keyframes modalPop {
    0% {
      opacity: 0;
      transform: translateY(40px) scale(.95);
    }

    100% {
      opacity: 1;
      transform: none;
    }
  }

  /* Кнопка закрытия */
  .modal-close {
    z-index: 99;

    position: absolute;
    top: 5px;
    right: 5px;

    background: none;
    border: none;
    font-size: 26px;
    line-height: 1;
    cursor: pointer;
    color: #999;

    transition: color .2s ease, transform .2s ease;
  }

  .modal-close:hover {
    color: #000;
  }

  /* Фото */
  .modal-agent__image {
    position: relative;
    max-height: 370px;
    overflow: hidden;
  }

  .modal-agent__image img {
    display: block;
    width: 100%;
    height: 158%;
    object-fit: cover;
    border-radius: 19px;
    transform: scale(1.05);

    transform: scale(1.05);
  }

  /* Контент */
  .modal-agent__content {
    padding: 28px;
    text-align: center;
  }

  .modal-agent__title {
    font-size: 22px;
    font-weight: 700;
    line-height: 1.3;
    margin-bottom: 12px;
    color: #555;
  }

  .modal-agent__title span {
    color: #229ED9;
  }

  .modal-agent__text {
    font-size: 15px;
    color: #555;
    margin-bottom: 22px;
  }

  /* CTA кнопка */
  .modal-agent__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;

    padding: 14px 28px;
    border-radius: 999px;

    background: linear-gradient(135deg, #229ED9, #1c8ec9);
    color: #fff;
    font-weight: 600;
    font-size: 16px;
    text-decoration: none;

    box-shadow: 0 12px 28px rgba(34, 158, 217, .4);

    transition:
      transform .25s ease,
      box-shadow .25s ease;
  }

  .modal-agent__btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 18px 36px rgba(34, 158, 217, .5);
  }

  .modal-agent__btn:active {
    transform: translateY(0);
    box-shadow: 0 8px 18px rgba(34, 158, 217, .35);
  }
</style>