<section class="section-comments" id="section-comments">
  <div class="container">
    <h2 class="section-title mb-3">Отзывы клиентов</h2>

    <div class="tg-comments-grid">
      <div class="tg-comment">
        <script async src="https://telegram.org/js/telegram-widget.js?22" data-telegram-post="AvtoraketaKZ/23406"
          data-width="100%"></script>
      </div>

      <div class="tg-comment">
        <script async src="https://telegram.org/js/telegram-widget.js?22" data-telegram-post="AvtoraketaKZ/23373"
          data-width="100%"></script>
      </div>

      <div class="tg-comment">
        <script async src="https://telegram.org/js/telegram-widget.js?22" data-telegram-post="AvtoraketaKZ/23372"
          data-width="100%"></script>
      </div>
    </div>

    <div class="text-center mt-4">
      <a href="https://t.me/AvtoraketaKZ/23338" class="tg-btn" target="_blank" rel="noopener">
        <span class="tg-btn__icon">✈️</span>
        <span class="tg-btn__text">Смотреть все отзывы в Telegram</span>
      </a>
    </div>
  </div>
</section>


<style>
  .tg-comments-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 24px;
  }

  .tg-comment {
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 8px 24px rgba(0, 0, 0, .08);
    background: #fff;
  }

  .tg-btn {
    --tg-color: #229ED9;

    display: inline-flex;
    align-items: center;
    gap: 12px;

    padding: 14px 28px;
    border-radius: 999px;

    background: linear-gradient(135deg,
        var(--tg-color),
        #1c8ec9);

    color: #fff;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;

    box-shadow:
      0 10px 24px rgba(34, 158, 217, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, .2);

    transition:
      transform .25s ease,
      box-shadow .25s ease,
      background .25s ease;
  }

  .tg-btn__icon {
    font-size: 18px;
    transition: transform .25s ease;
  }

  .tg-btn:hover {
    transform: translateY(-2px);
    box-shadow:
      0 16px 32px rgba(34, 158, 217, 0.45),
      inset 0 1px 0 rgba(255, 255, 255, .25);
  }

  .tg-btn:hover .tg-btn__icon {
    transform: translateX(4px);
  }

  .tg-btn:active {
    transform: translateY(0);
    box-shadow:
      0 8px 16px rgba(34, 158, 217, 0.3),
      inset 0 2px 6px rgba(0, 0, 0, .15);
  }

  /* адаптив */
  @media (max-width: 480px) {
    .tg-btn {
      width: 100%;
      justify-content: center;
      padding: 14px 20px;
      font-size: 15px;
    }
  }
</style>